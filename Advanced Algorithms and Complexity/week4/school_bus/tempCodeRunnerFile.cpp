!= 0)
    // {
    //     best_path.push_back(last);
    //     int nxt = last;
    //     for (int i = 0; i < n; i++) if( (all&(1<<i)) && last != i )
    //     {
    //         if(mem[all][last] == mem[all^(1<<last)][i]){
    //             all ^= (1<<last);
    //             last = i;
    //             break;
    //         }
    //     }
        
    // }
    // best_path.push_back(0);
    // reverse(best_path.begin(),best_path.end());
    